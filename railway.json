{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"${SERVICE_ROLE:-api}\" = worker ]; then python -m scripts.wait_for_db && exec python -m scripts.serve_worker; else python -m scripts.migrate && exec python -m scripts.serve_api; fi'",
    "healthcheckPath": "/health/live",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "numReplicas": 1,
    "sleepApplication": false
  }
}
